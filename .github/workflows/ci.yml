name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build testing image
        run: docker build --target testing -t catty-test .

      - name: Run server
        run: |
          docker run -d --name app -p 8181:8181 catty-test
          sleep 5

      - name: Run tests
        run: |
          docker run --rm --network host catty-test python -m pytest

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          target: production
          push: true
          tags: ghcr.io/sapdotten/catty-reminders-app:latest